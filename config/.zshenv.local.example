# Machine-specific environment variables — not checked into git
# Copy to ~/.zshenv.local and customize for each machine.

# SSH auth socket (e.g. for 1Password or other SSH agent)
# export SSH_AUTH_SOCK="/path/to/agent.sock"
